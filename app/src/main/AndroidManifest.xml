<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="tr.yildiz.myapplication">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".ShowCombineActivity"></activity>
        <activity android:name=".UpdatePartyActivity" />
        <activity android:name=".AddPartyActivity" />
        <activity android:name=".SelectImageDrawerActivity" />
        <activity android:name=".AllClothesActivity" />
        <activity android:name=".ListCombinesActivity" />
        <activity android:name=".ListCabinetActivity" />
        <activity
            android:name=".InsideDrawerActivity"
            android:label="Clothes List"
            android:parentActivityName=".AddClothesActivity" />
        <activity android:name=".AddDrawerActivity" />
        <activity android:name=".ListPartyActivity" />
        <activity android:name=".CabinetActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".AddClothesActivity" />
        <activity android:name=".ListDrawerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>